# SQL-Task-4
SQL queries using MySQL Workbench on the Sample Superstore dataset, including SELECT, WHERE, ORDER BY, GROUP BY, aggregate functions, subqueries, views, indexes, INNER JOIN, and LEFT JOIN.
